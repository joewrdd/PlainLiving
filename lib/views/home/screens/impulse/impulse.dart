import 'package:flutter/material.dart';

class ImpulseScreen extends StatelessWidget {
  const ImpulseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text('Impulse')]));
  }
}
