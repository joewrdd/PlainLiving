import 'package:flutter/material.dart';

class RestraintScreen extends StatelessWidget {
  const RestraintScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text('Restraint')]));
  }
}
